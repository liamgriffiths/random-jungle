'use strict';

import {} from './utils';
import CART from './cart';

export {create, predict};

const {floor, random} = Math;

const create = (X, Y, labels, opts = {}) => {
};

const predict = (x) => {
};

